#### 4.2.1.6. Execution Evidence for Sprint Review

La evidencia de ejecución disponible se limita a un fixture local del API
taggeado y a un smoke de emulador Android. No existe evidencia de ejecución
histórica de Sprint 1, Sprint Review, dispositivo físico ni producción.

##### LOCAL FIXTURE ONLY

El registro existente para un checkout aislado de API en tag `v0.17.0`, con
fixture local y workspace `icisa`, informa:

| Observed operation | Existing result | Scope relation | Limit |
| --- | --- | --- | --- |
| `POST /api/v1/authentication/sign-in` | HTTP 200 | `MOB-US-001`; `TS-MOB-001` | Fixture local; no Android en vivo ni entorno desplegado. |
| `GET /api/v1/session` | HTTP 200, `surface=PLATFORM`, usuario y Workspace presentes | `MOB-US-002`, `MOB-US-003` | Contexto observado del fixture; no prueba autorización productiva. |
| `POST /api/v1/authentication/refresh` | HTTP 200 y header refresh de reemplazo | `MOB-US-001` | Fixture local; no prueba ciclo de vida real de dispositivo. |
| `POST /api/v1/authentication/sign-out` | HTTP 204 | `MOB-US-001` | Fixture local; no prueba sesión de producción. |
| `GET /api/v1/skus/resolve?identifier=PROD-0001` | HTTP 200, `RESOLVED` con una candidata seleccionada por servidor | `MOB-US-011`; `TS-MOB-007` | No prueba cámara ni lectura física. |
| `GET /api/v1/skus/resolve?identifier=UNKNOWN-MOBILE-AV1` | HTTP 200, `NOT_FOUND` sin candidatas | `MOB-US-011`; `TS-MOB-007` | El fixture no aportó una corrida ambigua fija. |

##### EMULATOR ONLY

El registro existente del 2026-09-02 para `Nexa_AV1_API36` indica PASS para:

```text
android emulator start --cold Nexa_AV1_API36
android install --device emulator-5554 --apks=operations/build/outputs/apk/debug/operations-debug.apk
android run --device emulator-5554 --apks=operations/build/outputs/apk/debug/operations-debug.apk --activity=com.nexa.mobile.operations.MainActivity
android layout --device emulator-5554 --pretty
```

El estado observado fue instalación, launch y pantalla de acceso bloqueada sin
sesión local. **EMULATOR ONLY:** no prueba networking Android contra API,
cámara, decoding, SKU resolver en Android ni dispositivo físico.

**NOT EVIDENCED:** ejecución de cámara en hardware, evidencia física,
screenshots de Sprint Review, aceptación de producto y despliegue productivo.
